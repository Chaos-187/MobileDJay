<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You - MobileDJay</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-music me-2"></i>MobileDJay
            </a>
        </div>
    </nav>

    <main class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="text-center">
                    <div class="mb-4">
                        <i class="fas fa-check-circle fa-5x text-success"></i>
                    </div>
                    
                    <h1 class="display-4 text-success mb-3">Thank You!</h1>
                    
                    <div class="alert alert-success" role="alert">
                        <h5 class="alert-heading">
                            <i class="fas fa-user me-2"></i>Hi <%= customerName %>!
                        </h5>
                        <p class="mb-0">
                            Your <%= requestType %> has been successfully submitted to the DJ.
                        </p>
                    </div>

                    <% if (requestType === 'song request' && details) { %>
                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="fas fa-music me-2"></i>Requested Song
                            </h5>
                            <h6 class="text-primary"><%= details.title %></h6>
                            <p class="text-muted">by <%= details.artist %> - <%= details.genre %></p>
                        </div>
                    </div>
                    <% } %>

                    <% if (requestType === 'karaoke request' && details) { %>
                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="fas fa-microphone me-2"></i>Requested Karaoke Song
                            </h5>
                            <h6 class="text-warning"><%= details.title %></h6>
                            <p class="text-muted">by <%= details.artist %> - <span class="badge bg-<%= details.difficulty === 'Easy' ? 'success' : details.difficulty === 'Medium' ? 'warning' : 'danger' %>"><%= details.difficulty %></span></p>
                        </div>
                    </div>
                    <% } %>

                    <% if (requestType === 'message' && details) { %>
                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="fas fa-envelope me-2"></i>Your Message
                            </h5>
                            <p class="text-muted">"<%= details.message %>"</p>
                        </div>
                    </div>
                    <% } %>

                    <div class="alert alert-info" role="alert">
                        <i class="fas fa-info-circle me-2"></i>
                        The DJ will process your request shortly. Keep an eye out for your song!
                    </div>

                    <div class="d-grid gap-2 d-md-block">
                        <a href="/" class="btn btn-primary btn-lg">
                            <i class="fas fa-home me-2"></i>Back to Home
                        </a>
                        <% if (requestType === 'song request') { %>
                            <a href="/song-request?customerName=<%= encodeURIComponent(customerName) %>" class="btn btn-outline-secondary btn-lg">
                                <i class="fas fa-arrow-left me-2"></i>Make Another Song Request
                            </a>
                        <% } else if (requestType === 'karaoke request') { %>
                            <a href="/karaoke-request?customerName=<%= encodeURIComponent(customerName) %>" class="btn btn-outline-secondary btn-lg">
                                <i class="fas fa-arrow-left me-2"></i>Make Another Karaoke Request
                            </a>
                        <% } else if (requestType === 'message') { %>
                            <a href="/send-message?customerName=<%= encodeURIComponent(customerName) %>" class="btn btn-outline-secondary btn-lg">
                                <i class="fas fa-arrow-left me-2"></i>Send Another Message
                            </a>
                        <% } else { %>
                            <a href="/?customerName=<%= encodeURIComponent(customerName) %>" class="btn btn-outline-secondary btn-lg">
                                <i class="fas fa-arrow-left me-2"></i>Make Another Request
                            </a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-redirect after 30 seconds
        setTimeout(() => {
            window.location.href = '/';
        }, 30000);
    </script>
</body>
</html>
